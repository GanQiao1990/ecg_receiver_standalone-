# Core dependencies for the ECG receiver application
pyserial>=3.5
numpy>=1.24.0
PyQt5>=5.15.0
pyqtgraph>=0.13.0
python-dotenv>=1.0.0

# Dependencies for the Kivy GUI
kivy>=2.3.0

# Performance monitoring dependency
psutil>=5.9.0

# Networking and utility dependencies
requests==2.25.1
urllib3==1.26.6
certifi==2021.5.30
chardet==4.0.0
idna==2.10

# Development and testing dependencies (optional)
# Uncomment the following lines if you need them for development
# pytest>=7.0.0
# pytest-qt>=4.2.0

# Note: Legacy Tkinter GUI dependencies (customtkinter, matplotlib, Pillow)
# have been replaced with Kivy for better cross-platform compatibility.
# The Kivy GUI is now the recommended default interface.